#include <iostream>
#include <vector>
using namespace std;

void InsertionSort(vector<int> &arr, int size)
{
    int shift = 0, comp = 0, startingIndex, elementOnHand, currentIndex;
    for (startingIndex = 1; startingIndex < size; startingIndex++)
    {
        elementOnHand = arr[startingIndex];
        currentIndex = startingIndex - 1;

        while ()
        {
        }
    }
}

int main()
{
    return 0;
}